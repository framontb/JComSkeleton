{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Admin Build",
            "type": "shell",
            "command": "php ./vendor/bin/phing -f ./buildfiles/build.xml build -Dcomponent.name=joomprosubs",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}